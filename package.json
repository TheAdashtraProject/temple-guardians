{
  "name": "temple-guardians",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check dist/app.js && node --check dist/engine.js && node --check dist/data.js && node --check dist/campaign.js && node --check dist/renderer.js",
    "start": "python3 -m http.server 8080 --directory dist"
  }
}
